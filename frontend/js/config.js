// Change this one value when you deploy the backend somewhere else
// (e.g. Render, Railway, a VPS) — nothing else in the frontend needs editing.
// Example once deployed on Render: 'https://your-service-name.onrender.com/api'
const API_BASE_URL = 'http://localhost:5000/api';
